# 📱 iPad 3 复古仪表盘 (Weather Station)

> 让闲置的旧 iPad (iOS 9) 变身为精美的桌面时钟、日历与专业气象站。

本项目是一个纯静态的 HTML/JS 网页，专为 **iPad 3 / iPad 4 / iPad Mini 1** 等搭载 **iOS 9** 系统的老旧设备设计。它充分利用了 Retina 视网膜屏幕和 4:3 的屏幕比例，提供极致流畅的全屏体验。

## ✨ 核心特性

- **⚡️ 极致兼容**：纯原生 HTML5/CSS3/ES5 JavaScript 编写，无现代框架依赖，iOS 9 Safari 秒开。
- **📐 完美布局**：专为 1024x768 (4:3) 分辨率设计，左侧实时气象面板，右侧超大日历。
- **🌦 专业气象数据**：
  - 集成 **Open-Meteo** 免费 API（**无需 API Key**，无需注册）。
  - **6维数据面板**：实时显示温度、相对湿度、空气质量 (AQI)、风速风向、紫外线强度 (UV)、天气状况。
  - 数据直接通过 JS 本地请求，不依赖外部图片服务，稳定且节省流量。
- **📅 智能日历**：本地 JS 生成万年历，支持中文年月显示，自动高亮"今天"和"周末"。
- **📺 全屏模式**：支持 iOS Web App 标准，添加到主屏幕后隐藏地址栏，沉浸式显示。

## 🛠️ 配置说明 (修改城市)

默认坐标设置为 **中国·天津**。如果您需要修改为其他城市，请编辑 `index.html` 文件：

1. 打开 `index.html`
2. 搜索代码块中的 `配置：天津坐标`
3. 修改 `LAT` (纬度) 和 `LON` (经度) 变量

```javascript
// --- 配置：您的城市坐标 ---
// 可以通过 Google Maps 或 OpenStreetMap 查询经纬度
var LAT = 39.14; // 纬度 (Latitude)
var LON = 117.20; // 经度 (Longitude)
```

## 🚀 部署方式

由于是纯静态页面，您可以将其部署在任何 Web 服务器上。

### 方案 A：Vercel (推荐，自带 HTTPS)

1. Fork 本仓库到您的 GitHub
2. 在 Vercel 中导入本仓库
3. 点击 Deploy，获得访问域名

### 方案 B：Docker / Nginx (NAS 本地部署)

如果您想在群晖/威联通或 Linux 服务器上部署，请创建 `docker-compose.yml`：

```yaml
version: '3'
services:
  dashboard:
    image: nginx:alpine
    container_name: ipad_dashboard
    restart: always
    ports:
      - "8090:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
```

## 📱 在 iPad (iOS 9) 上设置指南

由于 iOS 9 系统较老，请务必按照以下步骤操作以获得最佳体验：

### 1. 解决 HTTPS 证书问题 (仅针对 Vercel 等 HTTPS 部署)

Vercel 使用 Let's Encrypt 证书，而 iOS 9 的根证书已于 2021 年过期。

**现象**：访问网页白屏或提示"无法验证服务器身份"。

**解决方法**：
1. 在电脑上下载 ISRG Root X1 证书 (.pem)
2. 通过邮件发送给自己，在 iPad 邮件应用中打开附件
3. 根据提示安装"描述文件"，直到显示已验证（绿色勾）

### 2. 全屏运行 (去除浏览器地址栏)

1. 在 iPad Safari 中打开部署好的网页
2. 点击顶部菜单栏的分享按钮 (方框向上箭头图标)
3. 选择"添加到主屏幕" (Add to Home Screen)
4. 回到桌面，点击新生成的图标，即可全屏运行

### 3. 防止屏幕自动锁定

进入 iPad 设置 -> 通用 -> 自动锁定，选择"永不"。

建议配合 iPad 支架并长期插上电源使用。

## 🧩 技术细节

- **Weather Data**: Open-Meteo (Free for non-commercial use)
- **Frontend**: HTML5, CSS3 (Flexbox), Vanilla JavaScript (ES5)
- **Fonts**: 使用 iOS 系统原生字体 (-apple-system)，无需加载外部字体文件

## 📄 许可证

MIT License
